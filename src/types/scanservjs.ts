export interface Context {
  // Context properties
}

export interface ScanRequest {
  // ScanRequest properties
}

export interface ScanResponse {
  // ScanResponse properties
}

export interface Device {
  id: string;
  name: string;
}

// Add other necessary types as needed
